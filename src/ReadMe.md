python -m venv .venv
python -m pip install -r requirements.txt

git init
# Show staging area: Not in staging area (red) or in (green)
git status
# Add to staging area
git add .
# Commit changes
git commit -m "Initial commit"
# Show log command
git log
...

git remote add origin https://github.com/Dkolomy/....
git push -u origin main


git branch name-of-branch
git merge name-of-branch

# show current branch
git branch
# change branch
git checkout name-of-branch





# Gitignore templates
https://github.com/github/gitignore
# Awesome-selfhosted
https://github.com/awesome-selfhosted
https://awesome-selfhosted.net/

